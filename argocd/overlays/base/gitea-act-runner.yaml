apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitea-act-runner
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: default
  sources:
    - repoURL: https://dl.gitea.com/charts/
      chart: actions
      targetRevision: 0.1.2
      helm:
        valueFiles:
          - $values/apps/helm/gitea-act-runner/values.yaml
    - repoURL: GIT_REPO_URL_PLACEHOLDER
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: mlops
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
