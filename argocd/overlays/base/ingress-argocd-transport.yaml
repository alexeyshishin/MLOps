apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ingress-argocd-transport
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "7"
spec:
  project: default
  source:
    repoURL: https://github.com/alexeyshishin/MLOps.git
    targetRevision: main
    path: ingress/main/argocd-transport
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
